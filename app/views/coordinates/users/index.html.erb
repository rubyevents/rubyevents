<%= render "shared/location_users",
      location: @location,
      users: @users,
      nearby_users: @nearby_users || [],
      state: nil,
      state_users: [],
      country: @country,
      country_users: @country_users || [] %>
