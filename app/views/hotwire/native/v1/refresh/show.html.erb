<%# Rendered as a no-op when the native app intercepts /hotwire/native/v1/refresh via the
    path configuration. The native RefreshAppFragment resets all tab navigators
    on display, so this body is never shown to the user. %>
