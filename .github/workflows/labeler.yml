name: Labeler

on:
  - pull_request_target

jobs:
  labeler:
    name: Labeler
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Label PRs
        uses: actions/labeler@v6
